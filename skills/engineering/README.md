# engineering

Helpers beside Goose lifecycle:

- `improve-codebase-architecture` — deepen survey (uses `goose/codebase-design`)
- `finding-duplicate-functions`, `resolving-merge-conflicts`

Clarity polish after green: **`goose/implement`** (`references/simplify.md`). Lifecycle: [`../goose/`](../goose/).
