---
name: bug-triage
description: Turn an incoming bug report into a clean, agent-ready issue (repro, severity, scope).
disable-model-invocation: true
metadata:
  area: wip
---

# Bug Triage

Status: **stub**.

On-ramp: raw report → structured issue. Not for tickets already produced by **create-tickets**.

Decide whether the next step is **create-tickets** (needs breakdown) or **implement** / diagnose path.

## Overlap note

Distinct from ops `triage-bug` and Matt `triage` — this stub owns *our* incoming-bug → Linear shape; reconcile or replace those when graduating.
