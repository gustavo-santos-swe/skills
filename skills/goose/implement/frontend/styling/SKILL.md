---
name: web-styling
description: Use when styling Next.js UI — Tailwind, CSS modules, tokens, or global styles. Aesthetic taste may also use design/frontend-design skills.
metadata:
  area: goose
---

# Styling

Status: **stub** — topic list below is what to define later (Goose conventions + examples). Keep SKILL.md short; push deep samples to `references/`.

## When to use

- New UI styling, design tokens, dark mode on web.
- **`implement`** loading this pack for a matching change.

## Topics to fill (checklist)

### Approach
- Tailwind (or other) — our default; global CSS rules
- Tokens / theme (CSS variables); dark mode strategy

### Component styling
- `cn`/variants (CVA) — when
- Avoiding style soup in server components

### Align with
- design area / frontend-design for visual quality; accessibility for contrast

## Don't
- Don't fight the design system with one-off hex values on product pages.
- Don't load huge CSS for a single island without need.

## References

Optional: `references/` for longer examples. Project-specific paths stay in the target repo `AGENTS.md`.

